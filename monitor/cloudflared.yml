services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel run
    networks:
      - cloudflare
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}

networks: 
  cloudflare:
    external: true